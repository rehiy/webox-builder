### Update: 2020-02-18 ###

export wk_node=node-v12.16.1

wk_find_node() {

    wk__apply https://nodejs.org/dist/${wk_node##*-}/ ${wk_node} .tar.gz

}

wk_make_node() {

    ./configure --prefix=${wbapp}

    make && make install

}

wk_inst_node() {

    local fine=${wbapp}/bin/node

    [ -e ${fine} ] || wk_find_node
    [ -e ${fine} ] || wk_make_node
    [ -e ${fine} ] || wk__error ${wk_node}

}
