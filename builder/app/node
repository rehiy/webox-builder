### Update: 2019-08-15 ###

export wk_node=node-v10.16.3

wk_find_node() {

    wk__apply https://nodejs.org/dist/${wk_node##*-}/ ${wk_node} .tar.gz

}

wk_make_node() {

    ./configure --prefix=${wbapp}

    make && make install

}

wk_inst_node() {

    [ -f ${wbapp}/bin/node ] || wk_find_node
    [ -f ${wbapp}/bin/node ] || wk_make_node
    [ -f ${wbapp}/bin/node ] || wk__error ${wk_node}

}
