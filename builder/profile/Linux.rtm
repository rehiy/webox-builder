export OS_TYPE=Linux

export PKG_CONFIG_PATH=${wblib}/lib/pkgconfig

if [ ! -x /usr/bin/gcc ]; then
    apt update
    apt install -y pkg-config unzip wget
    apt install -y build-essential python-dev
fi

ulimit -n 65536
