wk_install() {

    chown 0:0  ${wksbin}
    chmod 0755 ${wksbin}/*

    if [ -f ~/.bashrc ]; then
        echo "source /srv/webox/runtime" >> ~/.bashrc
    fi

}


wk_install_usage() {

    echo "install           add runtime to ~/.bashrc"

}
