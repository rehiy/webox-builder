# Redis configuration file.
#
# Note that in order to read the configuration file, Redis must be
# started with the file path as first argument:
#
# ./redis-server ${webox}/etc/redis/redis.conf

################################## INCLUDES ###################################

include ${webox}/etc/redis/conf.d/default.conf

include ${webox}/etc/redis/conf.d/override.conf
