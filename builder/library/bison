### Update: 2019-05-22 ###

export wk_bison=bison-3.4.1

wb_find_bison() {

    wb_apply http://ftp.gnu.org/gnu/bison/ ${wk_bison} .tar.xz

}

wb_make_bison() {

    ./configure --prefix=${webox}

    make && make install

}

wb_inst_bison() {

    local fine=${webox}/bin/bison

    [ -e ${fine} ] || wb_find_bison
    [ -e ${fine} ] || wb_make_bison
    [ -e ${fine} ] || wb_error ${wk_bison}

}
