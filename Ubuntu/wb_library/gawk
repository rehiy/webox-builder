export wk_gawk=gawk-4.1.4

wb_make_gawk() {

    wb_apply http://ftp.gnu.org/gnu/gawk/ ${wk_gawk} .tar.xz

    mkdir -p build && cd build

    ../configure --prefix=${webox}

    make && make install

}

wb_inst_gawk() {

    local fine=${webox}/bin/gawk

    [ -e ${fine} ] || wb_make_gawk
    [ -e ${fine} ] || wb_error ${wk_gawk}

}
