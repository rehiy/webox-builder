export wk_tiff=tiff-3.8.2

wb_make_tiff() {

    wb_apply http://dl.maptools.org/dl/libtiff/ ${wk_tiff} .tar.gz

    ./configure --prefix=${webox}

    make && make install

}

wb_inst_tiff() {

    local fine=${webox}/lib/libtiff.a

    [ -e ${fine} ] || wb_make_tiff
    [ -e ${fine} ] || wb_error ${wk_tiff}

}
