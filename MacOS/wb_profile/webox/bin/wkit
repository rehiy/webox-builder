#!/bin/bash

wkexec=${0##*/}

[ "${wkexec}" = "wkit" ] && {
    wkexec=${1}
    shift
}

wksbin=/srv/webox/bin
wktemp=/srv/webox/var
wkhome=/srv/webox/wkit

source /srv/webox/runtime

source ${wkhome}/wb_main
wk_run ${wkexec} "${@}"
